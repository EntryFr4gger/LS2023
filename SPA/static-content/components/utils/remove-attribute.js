/**
 * RemoveAttribute is a function that removes the "disabled" attribute on a given element attribute.
 *
 * @param {HTMLElement} attribute - The attribute to be enabled.
 */
export function RemoveAttribute(attribute) {
    return attribute.removeAttribute("disabled");
}
